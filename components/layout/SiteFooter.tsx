import Link from "next/link";
import { navItems, profile } from "../../data/profile";
export function SiteFooter() { return <footer className="site-footer"><div className="footer-grid"><div><p className="eyebrow">AI Product Builder</p><h2>让 AI 进入真实工作流。</h2><p>{profile.intro}</p></div><div><strong>导航</strong>{navItems.map((item)=><Link key={item.href} href={item.href}>{item.label}</Link>)}</div><div><strong>联系</strong><Link href={`mailto:${profile.email}`}>{profile.email}</Link><Link href={profile.github} target="_blank" rel="noopener noreferrer">GitHub</Link><span>微信 {profile.wechat}</span><Link href={profile.resumeUrl} target="_blank" rel="noopener noreferrer">查看简历</Link></div></div><div className="footer-bottom"><span>© {new Date().getFullYear()} {profile.name}. Built as a product, not a template.</span><Link href="#top">返回顶部 ↑</Link></div></footer> }
